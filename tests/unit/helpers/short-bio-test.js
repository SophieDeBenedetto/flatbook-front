import { shortBio } from '../../../helpers/short-bio';
import { module, test } from 'qunit';

module('Unit | Helper | short bio');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = shortBio(42);
  assert.ok(result);
});
